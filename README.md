# FSRS Flashcards

A console-based flashcard application written in Python, featuring a **custom implementation of the FSRS (Free Spaced Repetition Scheduler) algorithm**.

The application uses a local **SQLite database** to store flashcards, user data, and review history.

### Technologies

* Python
* SQLite
* Custom FSRS implementation
* Console / CLI
